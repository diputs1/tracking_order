package com.example.tracking_order.modules.order.enums;

import com.example.tracking_order.modules.order.enums.OrderStatus;

public enum OrderStatus {
    PENDING, CONFIRMED, PICKING, SHIPPING, DELIVERED, FAILED, RETURNING, RETURN_COMPLETED, REATTEMPT, CANCELLED
}
